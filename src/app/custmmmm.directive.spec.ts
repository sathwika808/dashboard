import { CustmmmmDirective } from './custmmmm.directive';

describe('CustmmmmDirective', () => {
  it('should create an instance', () => {
    const directive = new CustmmmmDirective();
    expect(directive).toBeTruthy();
  });
});
